# Canonical cu128 torch trio (plan D10). Exactly two consumers: base/Dockerfile
# and tools/build_sage_wheel.sh (via BUILD_WHEELS.md). Bumping any line here
# means re-running the wheel runbook before the next base build.
#
# There is no existing cu128 pin to copy: every cu128 template installs the
# UNPINNED stable trio today ("torch torchvision torchaudio" at
# comfyui-wan/Dockerfile:16-17; ltx2 and qwen-image are the same shape). This
# file pins the newest coherent stable trio published on the cu128 index,
# verified against https://download.pytorch.org/whl/cu128/ on 2026-08-12:
# torch 2.11.0+cu128 / torchvision 0.26.0+cu128 / torchaudio 2.11.0+cu128 all
# exist as cp312 manylinux wheels. It is version-identical to the cu130 trio
# minimax already pins and validated on H200 (comfyui-minimax/Dockerfile:21),
# so both CUDA variants ride one torch version and one sage ABI story.
# An unpinned install from this index resolves to this exact trio today, so
# pinning changes nothing for a fresh build; it only stops future drift.
#
# Confirmed 2026-08-13: torch 2.11.0 for cu128. The alternative was pinning to
# whatever the currently-live wan image happens to carry (unknowable from the
# repo; it froze whatever was stable on its last build day). 2.11.0 matches
# minimax's validated cu130 pin version-for-version.
--index-url https://download.pytorch.org/whl/cu128
torch==2.11.0
torchvision==0.26.0
torchaudio==2.11.0
